var prefix = "i=";
var t = new Thread(() ->{
   for(var i in 0..9){
      println(prefix+i);
   }
});
t.start();
t.join();

var list = ["a1", "a2", "b1", "c2", "c1"];

list.stream()
    .filter((s) -> s.startsWith("c"))
    .map((s) -> s.toUpperCase())
    .sorted()
    .forEach((s) -> println(s));